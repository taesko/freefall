{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "request/list_subscriptions",
  "title": "Body of the list_subscriptions API method sent from the client.",
  "type": "object",
  "properties": {
    "v": {
      "type": "string"
    },
    "api_key": {
      "type": "string"
    },
    "limit": {
      "type": "number",
      "minimum": 0,
      "maximum": 20
    },
    "offset": {
      "type": "number",
      "minimum": 0,
      "maximum": 9007199254740991
    },
    "fly_from": {
      "type": "string"
    },
    "fly_to": {
      "type": "string"
    },
    "sort": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": {
        "type": "object",
        "properties": {
          "column": {
            "type": "string",
            "enum": ["updated_at", "id", "fly_from", "fly_to", "date_from", "date_to", "plan"]
          },
          "order": {
            "type": "string",
            "enum": ["ASC", "DESC"]
          }
        },
        "required": ["column", "order"],
        "additionalProperties": false
      }
    }
  },
  "required": [
    "v",
    "api_key"
  ]
}